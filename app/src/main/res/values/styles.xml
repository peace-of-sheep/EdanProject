<resources>

    <style name="SplashTheme" parent="Theme.MaterialComponents.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_layer</item>
        <item name="android:statusBarColor">@color/colorPrimaryDark</item>
        <item name="android:navigationBarColor">@color/colorPrimaryDark</item>
    </style>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="TextInputDense.ExposedDropdownMenu" parent="Widget.MaterialComponents.TextInputLayout.FilledBox.Dense.ExposedDropdownMenu">
        <item name="android:layout_marginTop">@dimen/dim_nor</item>
        <item name="android:layout_marginStart">@dimen/dim_nor</item>
        <item name="android:layout_marginEnd">@dimen/dim_nor</item>
    </style>

    <style name="TextInputDense" parent="Widget.MaterialComponents.TextInputLayout.FilledBox.Dense">
        <item name="android:layout_marginTop">@dimen/dim_nor</item>
        <item name="android:layout_marginStart">@dimen/dim_nor</item>
        <item name="android:layout_marginEnd">@dimen/dim_nor</item>
    </style>

</resources>
